{
  "extends": [
    "//"
  ],
  "tasks": {
    "start": {
      "cache": false,
      "interactive": true,
      "persistent": true,
      "description": "Start the server backend",
      "with": [
        "types:generate"
      ],
      "dependsOn": [
        "^start"
      ]
    },
    "types:generate": {
      "cache": true,
      "description": "Generate TypeScript types from the local Supabase database",
      "dependsOn": [
        "^types:generate"
      ]
    }
  }
}
