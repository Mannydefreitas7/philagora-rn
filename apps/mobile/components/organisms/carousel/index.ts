import PhCarousel from "./component";
import { reducer, setPreviousIndex, setNextIndex, setVisibleItems } from "./slice";

export { PhCarousel, reducer as carouselReducer, setPreviousIndex, setNextIndex, setVisibleItems };
