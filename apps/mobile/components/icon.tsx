import { Calendar2, GlobalSearch, Home2, ProfileCircle, VideoPlay } from "iconsax-react-nativejs";

const icons = {
  home: Home2,
  search: VideoPlay,
  calendar: Calendar2,
  profile: ProfileCircle,
};
